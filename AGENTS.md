# Agent skills

### Issue tracker

Issues live as GitHub issues in `brentm5/local-logs`, via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Default canonical roles; `bug`/`enhancement`/`wontfix` reuse GitHub's built-in labels. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout (`CONTEXT.md` + `docs/adr/` at the repo root). See `docs/agents/domain.md`.
